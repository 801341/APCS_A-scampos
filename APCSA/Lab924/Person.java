
/**
 * Write a description of class PersonClass here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class Person
{
    public Person(String name)
    {
        int count = 0; 
        int ci = name.indexOf(",");
        int si = name.indexOf(" ");
        String fname,lname;
            //scannerTest str = new inputStr.substring(io + 1);
            if (ci != -1){
           fname = name.substring(0,si);
           if (name.lastIndexOf(" ") != si){
               lname = 
               hi = name.substring(si + 1, name.lastIndexOf(" "));
            }
           
           
           
        }
    
    }
    public int sampleMethod(int y)
    {
        // put your code here
        return x + y;
    }
}
